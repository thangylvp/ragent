# Demo integration tests

This suite will exercise the WebSocket stream, pipeline composition and a
browser smoke path. It uses mock SLM/cloud/execution adapters in CI and keeps
real-model and real-robot runs explicitly marked.
