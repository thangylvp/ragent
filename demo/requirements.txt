# Install these small web/runtime extras into the existing `mega-asr` environment.
# PyTorch, Transformers, soundfile, FireRedVAD and Silero are already owned by
# that environment and are deliberately not replaced here.
fastapi>=0.115,<0.140
uvicorn[standard]>=0.30
httpx>=0.27
omnivad==0.2.13
webrtcvad-wheels==2.0.14
